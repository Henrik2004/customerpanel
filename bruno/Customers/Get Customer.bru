meta {
  name: Get Customer
  type: http
  seq: 3
}

get {
  url: http://localhost:8080/customers/1
  body: none
  auth: none
}

tests {
  test("getCustomer", function() {
    const data = res.getBody();
    expect(res.getStatus()).to.equal(200);
  });
}
