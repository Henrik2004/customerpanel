meta {
  name: Update Comment
  type: http
  seq: 4
}

put {
  url: http://localhost:8080/comments/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
  {
    "user": 1,
    "text": "Test-Kommentar updated",
    "updatedBy": "0"
  }
}

tests {
  test("updateComment", function() {
    const data = res.getBody();
    expect(res.getStatus()).to.equal(200);
  });
}
