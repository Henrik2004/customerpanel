meta {
  name: Create Comment
  type: http
  seq: 2
}

post {
  url: http://localhost:8080/comments
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
  {
    "user": 1,
    "text": "Test-Kommentar",
    "createdBy": "0"
  }
}

tests {
  test("createComments", function() {
    const data = res.getBody();
    expect(res.getStatus()).to.equal(201);
  });
}
