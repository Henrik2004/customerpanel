meta {
  name: Get User By Name
  type: http
  seq: 6
}

get {
  url: http://localhost:8080/users/byname/Test
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

tests {
  test("getUser", function() {
    const data = res.getBody();
    expect(res.getStatus()).to.equal(200);
  });
}
