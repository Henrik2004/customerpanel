meta {
  name: RecreateTables
  type: http
  seq: 1
}

post {
  url: http://localhost:8080/settings
  body: json
  auth: none
}

body:json {
  {
    "action": "recreateTables",
    "secret": "27#!?X"
  }
}

tests {
  test("recreatedTables", function() {
    const data = res.getBody();
    expect(res.getStatus()).to.equal(200);
  });
}
