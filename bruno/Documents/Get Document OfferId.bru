meta {
  name: Get Document OfferId
  type: http
  seq: 6
}

get {
  url: http://localhost:8080/documents/offerid/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

tests {
  test("getDocument", function() {
    const data = res.getBody();
    expect(res.getStatus()).to.equal(200);
  });
}
