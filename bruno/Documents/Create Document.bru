meta {
  name: Create Document
  type: http
  seq: 2
}

post {
  url: http://localhost:8080/documents
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:multipart-form {
  name: TestDocument3
  offerId: 2
  createdBy: 1
  : @file(C:\Users\henri\Downloads\Test3.txt)
}

tests {
  test("createDocument", function() {
    const data = res.getBody();
    expect(res.getStatus()).to.equal(500);
  });
}
