meta {
  name: Update Document
  type: http
  seq: 4
}

put {
  url: http://localhost:8080/documents/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
  {
    "name": "TestDocument3Tested",
    "documentPath": "./assets/Test3.txt",
    "updatedBy": "1"
  }
}

tests {
  test("updateDocument", function() {
    const data = res.getBody();
    expect(res.getStatus()).to.equal(200);
  });
}
