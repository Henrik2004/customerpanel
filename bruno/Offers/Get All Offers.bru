meta {
  name: Get All Offers
  type: http
  seq: 1
}

get {
  url: http://localhost:8080/offers
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

tests {
  test("getAllOffers", function() {
    const data = res.getBody();
    expect(res.getStatus()).to.equal(200);
  });
}
