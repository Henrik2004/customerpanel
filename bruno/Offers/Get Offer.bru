meta {
  name: Get Offer
  type: http
  seq: 3
}

get {
  url: http://localhost:8080/offers/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

tests {
  test("getOffer", function() {
    const data = res.getBody();
    expect(res.getStatus()).to.equal(200);
  });
}
